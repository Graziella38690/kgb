{% extends 'base.html.twig' %}

{% block title %}Nouvelle cible
{% endblock %}

{% block body %}
	<h1>Crer une cible</h1>
<div class="row justify-content-center">
<a href="{{ path('app_cible_liste') }}" class="btn btn-secondary" role="button">Retour à la liste des cibles</a>
</div>

{{ include('/_form.html.twig') }}
	
{% endblock %}