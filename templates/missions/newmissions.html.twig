{% extends 'base.html.twig' %}

{% block title %}Nouvelle mission
{% endblock %}

{% block body %}
	<h1>Crée une mission</h1>
<div class="row justify-content-center">
<a href="{{ path('app_liste') }}" class="btn btn-success" role="button">Retour à la liste des missions</a>
</div>
{{ include('_form.html.twig') }}
	<a href="{{ path('app_liste') }}">back to list</a>
{% endblock %}